name: Redeploy

on:
  schedule:
    - cron: '0 */12 * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Redeploy
      run: |
        git commit --allow-empty -m "Redeploy"
        git push
